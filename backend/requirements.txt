# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# --- Data / ORM ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Auth / security ---
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Background jobs ---
celery==5.4.0
redis==5.2.1

# --- Object storage ---
minio==7.2.12

# --- Reporting / export ---
openpyxl==3.1.5
Jinja2==3.1.4
# PDF generation (Phase 4 — payslips / payment advice). Requires system libs
# (pango / fontconfig) installed in the Dockerfile.
weasyprint==63.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
